@model IEnumerable<HotelManagement.Mvc.Models.ReservationModels>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_ManagementLayout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    @*<tr>
        <th>
            @Html.DisplayNameFor(model => model.Events.Select(x => x.ArriveDate))
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Events.Select(x => x.DepatureDate))
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Events.Select(x => x.Price))
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Events.Select(x => x.ReservationState))
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Events.Select(x =>x.Room).FirstOrDefault())
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Client.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Client.Surname)
        </th>
        <th></th>
    </tr>*@

    @foreach (var item in Model)
    {
        <tr>
            @foreach (var _event in item.Events)
            {

            <td>
                @Html.DisplayFor(modelItem => _event.ArriveDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => _event.DepatureDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => _event.Price)
            </td>
            <td>
                @Html.DisplayFor(modelItem => _event.ReservationState)
            </td>
            <td>
                @Html.DisplayFor(modelItem => _event.Room.RoomName)
            </td>

            }
            <td>
                @Html.DisplayFor(modelItem => item.Client.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Client.Surname)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            </td>
        </tr>
    }

</table>
